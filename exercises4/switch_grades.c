# include <stdio.h>
# include <stdlib.h>

int main ( int argc, char *argv[])
{
        switch(grades)
        {
                case A :
                        printf("Excellent!");
                        break;
                case B :
                        printf(""Good);
                        break;
                case C :
                        printf("Not so bad");
                        break;
                case D :
                        printf("Could be worse");
                        break;
                case E :
                        printf("Maybe next time");
                        break;
                case F :
                        printf("No comment");
                        break;
                default :
                        printf("Try again");
        }
        return(0);
}

