#include <stdio.h>
#include <stdlib.h>

int** dynamic_matrix(size_t dim)
{
        int **mat; 
        mat = malloc(dim * sizeof (int));
        
        return a;
}

int main ()
{
        for (int i = 0; i < 3; i++)
        {
                for (int n = 0; n < 3; n++)
                {
                        printf("%d,",a[i][n]);
                }
                printf("\n");
        }
        return(0);
}
