#include <stdio.h>
#include <stdlib.h>

int main ()
{
        int a[5][5] = {1,2,3,4,5};

        for (int i = 0; i < 5; i++)
        {
                for (int n = 0; n < )
                printf("array a[%d] = %ls\n",i,a[i]);
        }

        return(0);
}
