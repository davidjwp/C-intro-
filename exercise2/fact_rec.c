# include <stdio.h>
# include <stdlib.h>

unsigned long fact_recursion(unsigned n)
{
        unsigned long i = i * n; 

}
int main (int argc, char *argv[])
{

        unsigned long i = 1;
                while (i < n)
                {
                        i++;                
                        printf("%ld\n",fact_recursion(atoi(argv[1])));
                }
        return(0);

}
